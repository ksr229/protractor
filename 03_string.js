var name = "Sushank";
console.log(name[1]);   //tring is treated as an array

var lastName = "         Rana     ";
console.log(name+" " +lastName); //Concatenation of two string 

console.log(name.slice(0,4));  //SLICING first index is inclusive and last index is exclusive

console.log(name.toUpperCase()); //UPPERCASE
console.log(name.toLowerCase()); //LOWERCASE
var fName = name +" "+ lastName.trim();  ///TRIM gets rid of the trailing and leading spaces.
console.log(fName) ; //
console.log(name.charAt(2));
console.log(name.indexOf('s'));


